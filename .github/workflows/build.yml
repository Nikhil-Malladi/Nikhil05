name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: $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  # Needed to get PR information, if any
          SONAR_TOKEN: $7dfd3946bfe1c518bd34e832ccc16e161ae904c8
